require "spec_helper"

describe SpreeLookbook do
  it "has a version number" do
    expect(SpreeLookbook::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
