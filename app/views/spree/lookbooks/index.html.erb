<div class="content">
  <div class="container">

    <div data-hook="taxon_sidebar_navigation" class="content-sidebar">
      <%= render partial: 'spree/shared/taxonomies' %>
    </div>

    <% @lookbooks.each do |l| %>
      <%= link_to image_tag(l.image.url(:large)), show_lookbook_path(@taxon, l) %>
    <% end %>

  </div>
</div>